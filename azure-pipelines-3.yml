trigger:
- main

pool:
  name: Default   # ou le nom de ton agent pool personnalisé

steps:
- script: terraform version
  displayName: 'Vérifier la version de Terraform'

- script: |
    cd angular-app-kubernetes/terraform
    ls -la
    terraform init
    terraform plan
  displayName: 'Terraform Init & Plan'
